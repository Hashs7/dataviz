const generateInitialState = () => {
    return {
        current: {
            vue: 6,
            scale: 1,
            mailAmount: 10,
            modal: false
        }
    };
};

export default generateInitialState;
