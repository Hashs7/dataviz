
/**
 *
 * @param state
 * @param action
 * @returns {*}
 */
const saved = (state = {}, action) => {
    switch(action.type) {
        default:
            return state
    }
};

export default saved;
